"use client";

type Props = {
  onClick: (value: "new" | "recommand" | "search") => void;
  section: "new" | "recommand" | "search";
};

export default function SectionTab({ onClick, section }: Props) {
  return <>
  
  </>;
}
